After cloning the repository, it should automatically install all dependencies from requirements.txt, set up a Python interpreter as well as a .venv.

If this doesn't happen:
You need to install Python (Tested with 3.11 & 3.13).
Then install the requirements.txt with "pip install -r path/to/requirements.txt" (either locally or .venv)

Start the application via main.py.
